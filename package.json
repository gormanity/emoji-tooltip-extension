{
  "name": "emoji-revealer",
  "version": "0.1.2",
  "description": "Browser extension that displays emoji names on hover",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "clean": "rm -rf dist",
    "generate-emoji-data": "node scripts/generate-emoji-data.mjs",
    "test": "node scripts/test-emoji-regex.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0",
    "sharp": "^0.33.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT",
  "private": true
}
